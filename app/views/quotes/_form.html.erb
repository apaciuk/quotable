<%= form_with model: quote do |form| %> 
    <% if quote.errors.any? %>
    <div class="error-message">
    <%= quote.errors.full_messages.to_sentence.capitalize %>
    </div>
    <% end %>

    <div class="form-group">
    <%= form.text_field :name, class: "form-control form-control-lg", placeholder: "Creative Quote..." %>
    <%= link_to "Cancel", quotes_path, class: "btn btn-warning btn-sm mt-2 mb-2" %>
    <%= form.submit class: "btn btn-dark btn-sm mt-2 mb-2" %>
    <% end %>

